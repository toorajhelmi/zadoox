{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @zadoox/shared build && pnpm --filter @zadoox/web build",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "rootDirectory": "code/packages/web",
  "env": {
    "NODE_ENV": "production"
  }
}

