{
  "buildCommand": "cd code && pnpm install --frozen-lockfile --include=dev && pnpm --filter @zadoox/web build",
  "outputDirectory": "code/packages/web/.next",
  "installCommand": "cd code && pnpm install --frozen-lockfile --include=dev",
  "framework": "nextjs"
}

